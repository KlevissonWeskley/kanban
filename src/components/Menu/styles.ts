import styled from 'styled-components'

export const MenuContainer = styled.div`
    display: flex;
    flex-direction: column;
    gap: 3.7rem;
    padding: 2rem;
`

export const LinksPagesContainer = styled.div`
    display: flex;
    flex-direction: column;
    gap: 2.25rem;
`

export const BaseLinks = styled.div`
    color: #fff;
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 1rem;
`